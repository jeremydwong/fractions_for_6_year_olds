import React from "react";
import ReactDOM from "react-dom/client";
import FractionExplorer from "./fraction-explorer.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FractionExplorer />
  </React.StrictMode>
);
